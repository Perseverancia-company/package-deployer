# Monorepo

Synchronization

- [ ] Create a system user
- [ ] The system user synchronizes the packages files with the monorepo
	- [ ] It then adds the files to git, commits and pushes to the remote
- [ ] Furthermore the monorepo is a good place to test if pnpm works alright
