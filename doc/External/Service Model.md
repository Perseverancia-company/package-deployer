# Service model

Instead of using a global app path for other users on the same computer, each server instance of perseverancia could host repositories on different computers and serve them and all the functionality that could be done with a global app path would be available for each server instance.

- [ ] Service model
